# ============================================================
# 生产环境配置（npm run build / vite preview）
#
# 生产环境前端一般和后端不在同一个进程里，接口地址有两种常见做法，二选一：
#
#   方案 A（推荐，也是下面默认的）：Nginx 把 /api 反向代理到后端，
#          前端页面和接口同源 —— VITE_API_BASE 保持 /api 即可，什么跨域都不用管。
#          注意 Nginx 要写 location /api/ 而不是 location /api：
#          后者是前缀匹配，会把 /apixxx 这类路径一起吞掉。
#
#   方案 B：前端直接请求后端地址（浏览器会真的跨域，需要后端放行前端域名）：
#          VITE_API_BASE=https://api.example.com/api
#          此时后端的 app.cors.allowed-origins 要配上生产前端域名。
# ============================================================

VITE_API_BASE=/api
